apiVersion: v1
kind: Service
metadata:
  name: signa-fe
  namespace: sigma
  labels:
    app: signa-fe
spec:
  selector:
    app: signa-fe
  ports:
  - name: http
    port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
