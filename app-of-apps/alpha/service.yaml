apiVersion: v1
kind: Service
metadata:
  name: alpha-frontend
  namespace: alpha
  labels:
    app: alpha-frontend
spec:
  selector:
    app: alpha-frontend
  ports:
  - name: http
    port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
