apiVersion: v1
kind: Service
metadata:
  name: random-web
  namespace: random
  labels:
    app: random-web
spec:
  selector:
    app: random-web
  ports:
  - name: http
    port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
