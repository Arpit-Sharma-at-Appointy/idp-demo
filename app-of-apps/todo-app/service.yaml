apiVersion: v1
kind: Service
metadata:
  name: frontendapp
  namespace: todo-app
  labels:
    app: frontendapp
spec:
  selector:
    app: frontendapp
  ports:
  - name: http
    port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
